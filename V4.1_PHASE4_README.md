# CatDrama Studio V4.1 — Phase4 Director QA

Phase4 接入完整 Scene 的第二道 GPT Director 审核。

流程：

```text
Director Plan Import PASS
↓
DIRECTOR_QA_REQUIRED
↓
DIRECTOR_QA_REQUEST.md
↓
GPT QA JSON
↓
Python deterministic validation
├─ PASS → Scene LOCKED → Shot01 READY → GENERATE_CURRENT_SHOT_PACKAGE
├─ REPLAN_REQUIRED → 回到 PLAN_CURRENT_SCENE
└─ NEED_NEW_CAMERA → 停在 CAMERA_REQUIRED
```

## 命令

```bash
python3.12 tools/run.py EP002
```

生成 QA Request。

`DIRECTOR_QA_REQUEST.md` 会内嵌完整 `Scene_Director_Plan_RESULT.json`，
QA 不再只看到 Shot 摘要；因此可以真正审核 why 字段、Frozen Moment、
Continuity、Scene End 与完整节奏。

保存 GPT 返回：

```text
Director_QA_RESULT.json
```

然后：

```bash
python3.12 tools/run.py EP002 --apply-director-qa episodes/EP002/director_state/Scene01/Director_QA_RESULT.json
```

## PASS 门槛

- scene_score >= 80
- 无 BLOCKER
- 无 MAJOR
- QA 不允许直接修改 Shot Plan

PASS 后由现有 Phase5 Runtime 接入 `GENERATE_CURRENT_SHOT_PACKAGE`。
