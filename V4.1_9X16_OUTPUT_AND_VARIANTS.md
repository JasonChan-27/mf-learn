# CatDrama Studio V4.1.3 — 9:16 Output and Production Variants

## 升级目标

本次升级修复 Scene 首 Shot 容易生成横屏的问题，同时避免要求现有 Master 资产批量返工。

架构采用双层资产：

- Canonical Master：保存身份、空间或摄影机真相，允许保持现有比例。
- Production Variant：同一 Master 的可选生产版本，例如 `portrait_9x16`。

最终 Shot Image、Video 和 Export 仍固定为 `9:16 / PORTRAIT / 1080x1920`。

## 安装

本补丁直接应用于用户上传的 V4.1.1，不需要安装此前的 V4.1.2 补丁。

1. 将补丁 ZIP 解压到 `catdrama-studio` 项目根目录。
2. 覆盖同名文件。
3. 不要执行 `--reset`。
4. 执行：

```bash
python3.12 tools/run.py EP002 --build
```

补丁前尚未批准的 Shot Package 会自动重新排队并生成新版本；旧 Package 不删除。

## 横屏 Master 的处理

横屏 Camera/Environment 不再触发 `NEED_ASSET`。Builder 会继续打包 Canonical Master，并记录非阻塞提示：

```text
PORTRAIT_VARIANT_RECOMMENDED
```

该提示不改变 Runtime，不要求停产，也不允许 Python 裁图、换 Camera 或改 Composition。

## 可选 portrait_9x16 Variant

常用 Camera 可以逐步增加竖屏 Production Variant。假设现有文件为：

```text
assets/cameras/Entrance Hall Camera01.png
```

新增：

```text
assets/cameras/Entrance Hall Camera01_Portrait_9x16.png
assets/cameras/Entrance Hall Camera01.asset.json
```

Sidecar 内容：

```json
{
  "id": "cameras.entrance-hall-camera01",
  "variants": {
    "portrait_9x16": "Entrance Hall Camera01_Portrait_9x16.png"
  }
}
```

如果 Sidecar 已存在，只添加 `variants` 字段，不删除已有 `id`、`name`、`aliases` 或 `tags`。

执行 `--build` 后：

- `AssetRegistry.json` 仍只有原 Camera Asset ID；
- Variant 不会被注册成第二个 Camera；
- Director Plan 仍选择 `cameras.entrance-hall-camera01`；
- Builder 确定性优先打包 `portrait_9x16`；
- Manifest 同时记录 Canonical Master 与实际使用的 Variant；
- Python没有进行新的 Camera、Coverage 或 Composition 决策。

## Prompt 和 QA

Compiler 在每个 IMAGE/VIDEO Prompt 中自动写入一次统一画幅合同。每次生成请求相互独立，因此该重复是必要的；用户不需要手工维护。

IMAGE Prompt 还会明确：

- Camera Master 决定位置、方向、高度和镜头意图，不决定源图片边界；
- Environment Master 决定空间真相，不决定输出比例；
- 横屏参考必须在原生竖屏画布中重新组织，不能输出横屏或黑边。

Prompt Contract QA 会拒绝缺少画幅合同的 Prompt。`--approve-image` 会拒绝横屏 Final Image。

## 不会覆盖的内容

- `assets/` 与 `AssetRegistry.json`
- `episodes/EP002/director_state/`
- 已批准的图片、视频和旧 Package
- `build/`
- `.git/`

## 验证

```bash
python3.12 -m unittest discover -s tests -p 'test_*.py'
```

本次交付：41 项通过，1 项因上传快照未包含 Master 二进制资产而跳过；完整 Assets 环境仍会严格执行该项。
