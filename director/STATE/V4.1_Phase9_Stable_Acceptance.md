# CatDrama Studio V4.1 — Phase9 Stable Acceptance

## 验收结论

EP002 Scene01 已在隔离副本中完成真实 Master 资产与正式 V4.1 Runtime 全链回归：

```text
Scene01 PLANNING / Shot Count 0
→ GPT Director Plan（4 Shots）
→ Director Plan Import PASS
→ GPT Director QA PASS
→ 4 × Pure Compiler / Versioned Builder
→ 4 × Image Approval / Video Approval Runtime Transition
→ Scene02 PLANNING / Shot Count 0
```

正式项目本身仍保持 `Scene01 / PLANNING / Shot Count 0`，验收用 Plan、QA 和占位
审批媒体没有写入正式 Episode State。

## Director 验收

- Shot 数量由 GPT Director 根据锁定剧本与节奏决定为 4，Python 未预设。
- Camera、Coverage、Composition、Camera Motion 均由 GPT 明确选择。
- 每镜都有 `why_this_shot` 与 `why_not_previous_shot`。
- 每个 Frozen Moment 都有可见物理边界与禁止提前状态。
- 相邻镜头的 Function / Focus / Coverage / Information Density 均有导演差异，
  没有连续同构图微动作拆镜。
- 最后一镜 `scene_end = true`、`next_shot = Scene02`。
- Confirmed Script 的全部 Scene01 对白原样进入锁定 Shot Delta。

## Asset / Builder 验收

- 83 个 Master 与 `AssetRegistry.json` 的 ID、路径、大小、SHA256 全一致。
- EP002 Episode Selection：26 selected / 0 missing / 0 ambiguous。
- Scene01 每个 Shot Package 只包含 7 个明确资产：玄关、四角色、小南服装、
  Entrance Hall Camera01。
- 四个 Package 均为 `v001`，0 blocking，未复制无关资产。

## Runtime 验收

- Scene01 四个 Shot 全部按锁定 `next_shot` 推进。
- Scene End 后 Scene02 自动进入 `DRAFT / PLANNING / Shot Count 0`。
- Scene 切换会清空上一 Scene 的 pending Plan / QA 路径。
- Runtime pending 路径使用 Episode 相对路径，不写入不可移植的本机绝对路径。
- 下一 Scene 的 `updated_at` 与 State Index 同步刷新。

## 媒体范围

Phase9 使用可识别 PNG 作为隔离审批接口占位文件，只验证 Runtime 与 Builder，
不将其视为视觉生成 QA 通过，也不随正式项目交付。正式图片与视频仍需用户按每个
Shot Package 生成并人工审核。

