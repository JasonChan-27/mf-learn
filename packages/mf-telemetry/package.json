{
  "name": "mf-telemetry",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts",
    "check": "pnpm typecheck && pnpm lint",
    "build": "tsup"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
