# CatDrama Studio V4.1 — Phase6 Pure Prompt Compiler

Phase6 将 Prompt Compiler 从 Production Runtime 中彻底拆出。

新增：

```text
tools/state_prompt_compiler.py
tools/prompt_contract_qa.py
```

修改：

```text
tools/production_runtime.py
```

## 新职责

`state_prompt_compiler.py`

只做：

```text
Shot State
+
Previous Frame Policy
↓
IMAGE_PROMPT.md
VIDEO_PROMPT.md
CHATGPT_HANDOFF.md
PROMPT_COMPILATION_MANIFEST.json
```

不允许重新导演。

`prompt_contract_qa.py`

确定性检查：

- Camera 是否写入 IMAGE Prompt
- Coverage 是否写入
- Composition Intent 是否写入
- Frozen Moment 是否完整
- Physical Boundaries 是否完整
- Forbidden Advanced States 是否完整
- Shot Delta 是否完整进入 VIDEO
- Shot Delta 主动作不得进入 IMAGE
- Shot Delta 的 Secondary Action / Performance / Timing / End State / Camera Motion 不得进入 IMAGE

Prompt Compiler 不读取 `DirectorDecision`。导演理由用于 QA 和问题归因；
生产 Prompt 的唯一执行真相仍是 `Shot_State.json`。

`PROMPT_COMPILATION_MANIFEST.json` 记录：

- `compiler_mode = PURE_STATE_TRANSLATION`
- `creative_decision_allowed = false`
- immutable prompt source state SHA256
- IMAGE / VIDEO / Handoff SHA256
- 编译器消费的 locked field 列表

## 验证

```bash
python3.12 -m unittest discover -s tests -v
```

当前 Phase6 回归覆盖纯编译、VIDEO-only 泄漏拦截、hash 稳定性，
显式角色/服装资产映射、Camera / Asset 阻塞，以及
Package → Image Approval → Video Approval → Episode Complete 状态链。

最新完整素材项目同时验证：83 项素材可被扫描，EP002 请求的 26 项素材
全部唯一解析（missing=0，ambiguous=0）。

## 目标

进一步保证：

> GPT Director 决策一次，JSON 保存一次，Python 只翻译，不再二次创作。

Phase7 Builder 现已完成。资产复制、`upload/`、`required_assets.json` 与
Package 版本化职责已经从 Production Runtime 收束到正式 Builder。

详见：`V4.1_PHASE7_README.md`。
