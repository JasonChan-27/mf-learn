# CatDrama Studio V4.1 — Phase 1 State Schema

基线：`CatDrama-Studio(14).zip`

本阶段只建立 V4.1 的 JSON 合同，不改 Runtime Python 行为。

## 新增

```text
director/STATE/Scene_Director_Plan.schema.json
director/STATE/Director_Decision.schema.json
director/STATE/V4.1_State_Architecture_Delta.md
```

## 升级

```text
director/STATE/Scene_Plan.schema.json
director/STATE/Shot_State.schema.json
director/STATE/Runtime_State.schema.json
```

## 关键变化

- GPT Director Plan 成为镜头创意唯一来源。
- `why_this_shot` / `why_not_previous_shot` 成为强制字段。
- `composition_intent` 成为强制字段。
- `camera_motion_reason` 成为强制字段。
- Frozen Moment 的 `physical_boundaries` 与 `forbidden_advanced_states` 均必须由 GPT Director 提供。
- Shot State 只保存可执行结果，不允许 Python 创作性补全。
- Runtime Schema 已为 V4.1 新状态做好准备。

## 本阶段为什么不修改 Python

因为接下来 Phase 2 会专门修改：

```text
tools/director_state.py
tools/build_episode.py
```

目标是：

```text
python3.12 tools/run.py EP002 --reset
```

之后 **Scene01 也必须进入 PLANNING**，不再读取 `Scene01_Plan.example.json` 自动锁 4 个 Shot。

Schema 先冻结，后续所有代码严格按 Schema 实现。
