# CatDrama Studio V4.1 — Phase7 Builder

Phase7 已将 Shot Package 构建职责从 `production_runtime.py` 收束到：

```text
tools/build_shot_package.py
```

## 已完成

- Builder 只消费锁定 Runtime / Scene State / Shot State。
- Prompt 继续由 Phase6 Pure Prompt Compiler 生成。
- 只复制当前 Shot 明确声明的 Master。
- Registry 有记录但源文件缺失、大小不符或 SHA256 不符时显式阻塞。
- `NONE`、`STATE_REFERENCE_ONLY`、`HIGH_PRIORITY_VISUAL_CONTINUITY` 三条路径确定性执行。
- 生成 `prompt.md`、`required_assets.json`、`upload/` 及完整 Manifest。
- Package 使用 `v001 / v002 / ...` 版本目录。
- 相同输入幂等复用；输入改变或 Package 损坏时创建新版本，不覆盖旧版本。
- `production_runtime.py` 只保留 Image/Video Approval 和 Shot/Scene 推进。
- `run.py` 仍是唯一日常入口。

## Package 路径

```text
episodes/<EP>/production/<Scene>/<Shot>/packages/v001/
```

当前有效版本记录在：

```text
episodes/<EP>/production/<Scene>/<Shot>/CURRENT_PACKAGE.json
```

批准后的 Final Image / Video 仍保存在 Shot 根目录，使下一 Shot 能按锁定的
Previous Frame Policy 稳定读取。

## 验证

```bash
python3.12 -m unittest discover -s tests -p 'test_*.py' -v
```

当前 Phase1～Phase7 共 16 项自动测试全部通过。

Phase8 Workflow 已完成：`00_START_HERE.md`、`00_WORKFLOW.md` 与
`director/Production_Standard.md` 已成为 V4.1 正式入口，并统一 Python 3.12
命令。下一阶段为 Phase9 EP002 真实资产回归。
