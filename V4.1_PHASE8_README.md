# CatDrama Studio V4.1 — Phase8 Workflow

Phase8 已将项目正式操作面收口到 V4.1 Runtime 工作流。

## 已完成

- 重写 `README.md`、`00_START_HERE.md` 与 `00_WORKFLOW.md`。
- 新增 `director/Production_Standard.md` 作为 V4.1 正式职责合同。
- 所有正式命令统一为 `python3.12 tools/run.py ...`。
- 明确最低 Python 3.10、推荐 Python 3.12。
- Build / Reset / Status、Planning、Director QA、Builder、Image / Video Approval、
  `NEED_NEW_CAMERA`、`NEED_ASSET`、Next Scene 全部纳入唯一工作流。
- Context Pack 与 Upload Manifest 只请求 GPT Director Plan / QA 纯 JSON，不再请求
  GPT 一次性生成完整 Prompt。
- V2/V4 的 Production Stage Prompt、Output Spec 与旧 Prompt Quality Gate 已退出
  正式构建包，并保留历史标记供追溯。
- `run.py` 仍是唯一日常入口；Director Plan、Shot State、Runtime State 的唯一真
  相地位不变。

## 正式入口

```bash
python3.12 tools/run.py EP002 --build
python3.12 tools/run.py EP002
```

EP002 仍保持 `Scene01 / PLANNING / Shot Count 0`，Phase8 没有替 GPT Director
创建任何 Shot。

## 验证

```bash
python3.12 -m unittest discover -s tests -p 'test_*.py' -v
python3.12 tools/validate_project.py
python3.12 tools/run.py EP002 --build
```

Phase1～Phase8 共 22 项自动测试全部通过。仅保留 `AssetRegistry.json`、未包含
Master 图片的源码包也能完成 Build，并确定性报告 EP002 的 26 个缺失资产；这不
会触发 Python 猜测或替代资产。

Phase9 EP002 真实资产回归已完成，项目已进入 V4.1 Stable。详见
`V4.1_PHASE9_README.md`。
