# CatDrama Studio V4.1 — Phase1～Phase5 Interface Audit

审计日期：2026-08-24

## 架构结论

核心边界保持不变：GPT Director 决定 Shot 数量、Camera、Coverage、Composition、
Camera Motion、Frozen Moment 与 Shot Delta；Python 只负责状态、合同验证、编译、
文件 IO、Package 与 Runtime 推进。

## 审计结果

| Phase | 原实现状态 | 发现的接口问题 | 当前处理 |
| --- | --- | --- | --- |
| Phase1 State Schema | 主合同已建立 | Runtime 枚举与真实写入状态不一致；Shot 批准字段、Scene Plan QA 字段被 `additionalProperties: false` 拒绝 | 已统一为 V4.1 正式状态并补齐实际持久化字段 |
| Phase2 Build / Reset | Reset 后所有 Scene 均为 PLANNING，Shot Count=0 | 新环境依赖未声明的 `jsonschema` | 已加入项目内确定性 Schema 校验器，无第三方依赖 |
| Phase3 Plan Import | Plan → Decision / Scene Plan / Shot State 主链存在 | Python 根据 Composition 拼造 `coverage_reason`、根据自然语言猜 `axis_status`；Director Plan 缺少 Character Master 显式映射；未检查 Runtime 的 next scene；重规划可能残留旧 Shot 文件 | 导演字段改为 GPT 必填并原样复制；新增 `character_assets` 显式映射；补 next_scene / next_shot Gate；每次成功编译先清理候选旧状态 |
| Phase4 Director QA | PASS / REPLAN / CAMERA 分支存在 | `run.py` 没有生成 QA Request；Request 只有摘要，无法审核 Frozen Moment 等完整内容 | `run.py` 自动生成；Request 内嵌完整 Director Plan；Camera Gap 生成 `CAMERA_REQUIREMENT.md` |
| Phase5 Production Runtime | Package、图片/视频批准、Scene 推进主链存在 | Runtime Schema 不接受 `PRODUCTION` / `WAIT_FOR_VIDEO_APPROVAL`；Episode 完成值漂移；缺资产仍可能推进；Director State Index 长期陈旧 | Schema 与 Runtime 统一；缺资产进入 `NEED_ASSET` 并可重试；Index 与 Scene State 同步 |

## 通过的接口回归

独立临时项目中完整执行：

```text
RESET
→ PLAN_CURRENT_SCENE
→ APPLY_DIRECTOR_PLAN
→ DIRECTOR_QA_REQUIRED
→ APPLY_DIRECTOR_QA PASS
→ GENERATE_CURRENT_SHOT_PACKAGE
→ WAIT_FOR_IMAGE_APPROVAL
→ WAIT_FOR_VIDEO_APPROVAL
→ Scene01 COMPLETED
→ Scene02 PLAN_CURRENT_SCENE
```

另外验证：

- `NEED_NEW_CAMERA` 会生成 Camera Requirement，不会自动选择近似 Camera。
- `NEED_ASSET` 会生成 Asset Requirement；补齐 Master 后可由同一 `run.py` 恢复。
- Director State Index 与 Scene State 的 LOCKED / COMPLETED / Shot Count 同步。
- QA Request 包含完整 Director Plan，而不是只含 Shot 摘要。
- 最新项目素材目录扫描得到 83 项资产；EP002 声明的 26 项需求全部可唯一解析，
  缺失 0、歧义 0。
- 可见/部分可见角色必须由 GPT Director 在 `character_assets` 中显式指定
  Character Master；Python 只复制该映射，不按角色名猜资产。

## 未进入本阶段的内容

Phase7 Builder、Phase8 Workflow 文档重写、Phase9 EP002 真实导演回归仍按原路线图保留，
本次没有提前改变其职责边界。
