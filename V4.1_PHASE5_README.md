# CatDrama Studio V4.1 — Phase5 Production Runtime

本阶段接入真正的 Shot Production Runtime。

主要解决：

1. 切镜不再强制参考上一 Final。
2. SAME_CAMERA_CONTINUATION 才使用 HIGH_PRIORITY_VISUAL_CONTINUITY。
3. IMAGE Prompt 严格停在 Frozen Moment；动作发展只进入 VIDEO Prompt。
4. 每个 Shot 生成 `CHATGPT_HANDOFF.md`，明确 `SHOT_PROMPT_ONLY`，避免 ChatGPT 自动继续生成图片。
5. Image / Video 批准后 Runtime 自动推进 Next Shot。
6. 最后一 Shot 完成后自动识别 Scene End，并进入下一 Scene Planning。
7. 最后一 Scene 完成后进入 EPISODE_COMPLETE。

## 生产命令

当 Runtime 为 `GENERATE_CURRENT_SHOT_PACKAGE`：

```bash
python3.12 tools/run.py EP002
```

生成：

```text
production/SceneXX/ShotXX-XX/
├── IMAGE_PROMPT.md
├── VIDEO_PROMPT.md
├── UPLOAD_MANIFEST.json
└── CHATGPT_HANDOFF.md
```

图片通过后：

```bash
python3.12 tools/run.py EP002 --approve-image path/to/ShotXX-XX_Final.png
```

视频通过后：

```bash
python3.12 tools/run.py EP002 --approve-video path/to/video.mp4
```

或只推进状态：

```bash
python3.12 tools/run.py EP002 --approve-video-only
```

若当前 Shot 缺少 Master，Runtime 进入 `NEED_ASSET` 并生成
`ASSET_REQUIREMENT.md`。补齐资产后再次运行 `run.py` 会确定性重试，
不会由 Prompt 临时设计缺失资产。
